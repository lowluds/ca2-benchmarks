diff --git a/continuum/application.cpp b/continuum/application.cpp
index b91db00..2eb8572 100644
--- a/continuum/application.cpp
+++ b/continuum/application.cpp
@@ -34,6 +34,7 @@ namespace app_graphics3d_continuum
 
       m_pmainscene = nullptr;
       m_bAbsoluteMousePosition = false;
+      m_iSkyboxSelected = -1;
       m_ppaneimpact = nullptr;
       m_bGpu = true;
       //m_bImaging = false;
@@ -272,6 +273,8 @@ namespace app_graphics3d_continuum
          if (m_straSkybox[i] == m_strSkybox)
          {
             m_checkboxaSkyBox[i]->set_check(e_check_checked, ::e_source_sync);
+            // NOTE: Cache current selection index to avoid O(n) uncheck loops on each user click.
+            m_iSkyboxSelected = i;
          }
 
          pcheckbox->check_changed(this) += [this, i](::data::check_change& change)
@@ -288,24 +291,24 @@ namespace app_graphics3d_continuum
 
                if (bChecked)
                {
-
-                  for (int j = 0; j < m_straSkybox.size(); j++)
+                  // NOTE: Keep radio-button behavior in O(1) time by only unchecking the previous selection.
+                  if (m_iSkyboxSelected >= 0
+                     && m_iSkyboxSelected < m_checkboxaSkyBox.size()
+                     && m_iSkyboxSelected != i)
                   {
 
-                     if (j != i)
-                     {
-
-                        m_checkboxaSkyBox[j]->set_check(e_check_unchecked, ::e_source_sync);
-
-                     }
+                     m_checkboxaSkyBox[m_iSkyboxSelected]->set_check(e_check_unchecked, ::e_source_sync);
 
                   }
+                  m_iSkyboxSelected = i;
 
                }
                else
                {
 
+                  // NOTE: Prevent "no selection" state and preserve one active skybox option.
                   m_checkboxaSkyBox[i]->set_check(e_check_checked, ::e_source_sync);
+                  m_iSkyboxSelected = i;
 
                }
 
diff --git a/continuum/application.h b/continuum/application.h
index 79c4fd1..ab5b2aa 100644
--- a/continuum/application.h
+++ b/continuum/application.h
@@ -18,6 +18,7 @@ namespace app_graphics3d_continuum
 
       ::string_array m_straSkybox;
       ::string m_strSkybox;
+      int m_iSkyboxSelected;
 
       ::pointer_array < ::user::check_box> m_checkboxaSkyBox;
 
