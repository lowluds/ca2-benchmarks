diff --git a/continuum/CMakeLists.txt b/continuum/CMakeLists.txt
index 396e7f4..1b2a392 100644
--- a/continuum/CMakeLists.txt
+++ b/continuum/CMakeLists.txt
@@ -8,10 +8,7 @@ set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
 #add_subdirectory("vendor/glfw")
 
 find_package (Vulkan REQUIRED)
-#set(CMAKE_CXX_STANDARD 17)
-
-set(Vulkan_INCLUDE_DIR "C:/VulkanSDK/1.4.313.0/Include")
-set(Vulkan_LIBRARY "C:/VulkanSDK/1.4.313.0/Lib/vulkan-1.lib")
+# NOTE: Keep Vulkan paths toolchain-driven; hard-coded SDK paths break portability.
 include_directories(${Vulkan_INCLUDE_DIR}) 
 
 list(APPEND library_source
@@ -185,15 +182,33 @@ add_dependencies(${PROJECT_NAME} ${default_networking})
 add_dependencies(${PROJECT_NAME} ${default_write_text})
 add_dependencies(${PROJECT_NAME} database_sqlite3)
 
-#set(SPIRV_OUTPUT_DIR "${CMAKE_CURRENT_SOURCE_DIR}../_matter/continuum/_std/_std/shaders/spirV")
-#file(MAKE_DIRECTORY ${SPIRV_OUTPUT_DIR})
+set(SPIRV_OUTPUT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../_matter/continuum/_std/_std/shaders/spirV")
+file(MAKE_DIRECTORY ${SPIRV_OUTPUT_DIR})
+
+set(GLSLANG_VALIDATOR "${Vulkan_GLSLANG_VALIDATOR_EXECUTABLE}")
+# NOTE: Resolve glslangValidator from Vulkan package, then VULKAN_SDK, then PATH.
+if (NOT GLSLANG_VALIDATOR)
+    if (DEFINED ENV{VULKAN_SDK})
+        if (WIN32)
+            set(_glslang_candidate "$ENV{VULKAN_SDK}/Bin/glslangValidator.exe")
+        else ()
+            set(_glslang_candidate "$ENV{VULKAN_SDK}/bin/glslangValidator")
+        endif ()
+        if (EXISTS "${_glslang_candidate}")
+            set(GLSLANG_VALIDATOR "${_glslang_candidate}")
+        endif ()
+    endif ()
+endif ()
+if (NOT GLSLANG_VALIDATOR)
+    find_program(GLSLANG_VALIDATOR glslangValidator REQUIRED)
+endif ()
 
 foreach(SHADER_FILE ${SHADER_FILES})
     get_filename_component(FILE_NAME ${SHADER_FILE} NAME)
     add_custom_command(
             OUTPUT ${SPIRV_OUTPUT_DIR}/${FILE_NAME}.spv
             COMMAND ${CMAKE_COMMAND} -E echo "Compiling shader: ${FILE_NAME}"
-            COMMAND glslangValidator -V ${SHADER_FILE} -o ${SPIRV_OUTPUT_DIR}/${FILE_NAME}.spv
+            COMMAND ${GLSLANG_VALIDATOR} -V ${SHADER_FILE} -o ${SPIRV_OUTPUT_DIR}/${FILE_NAME}.spv
             DEPENDS ${SHADER_FILE}
             COMMENT "Compiling ${FILE_NAME} to SPIR-V"
     )
@@ -219,4 +234,4 @@ target_include_directories(static_${PROJECT_NAME} PRIVATE ${library_include_dire
 
 target_compile_definitions(${PROJECT_NAME} PRIVATE _${PROJECT_NAME}_project)
 
-add_subdirectory(__implement)
\ No newline at end of file
+add_subdirectory(__implement)
